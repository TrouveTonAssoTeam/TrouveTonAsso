<div style="text-align: center;">
    <h1><strong>Trouve Ton Asso</strong></h1>
    <br>
    <p>Chez Touve Ton Asso, nous croyons que chaque acte de <strong>générosité</strong>, quel que soit sa taille, a le <strong>pouvoir de transformer des vies</strong>. Notre plateforme est bien plus qu'un simple espace pour faire des dons ; c'est un lieu où l'empathie prend vie, où des causes inspirantes trouvent un écho, et où l'impact concret devient <strong>accessible à tous</strong>.</p>
    <%= link_to "En Savoir Plus", about_path, class: "nav-link", style: "text-decoration: none;" %>
</div>


<br>
<br>

<% if !@promoted.empty? %>
    <h1 class="mt-6" style="text-align: center; margin-top: 10px;">Les associations mises en avant</h1>
    <p style="text-align: center;">En soutenant le site vous avez la possibilité de mettre en avant votre association. 
    <br>
    Démarquez vous du reste et augmentez votre visibilité.</p>
    <hr style="width: 50%; margin-bottom: 30px;">

    <div class="inline-card-container gold-container">
        <% @promoted.each do |organisation| %>
            <%= link_to organisation_path(organisation), class: "card card-gold" do %>
            
                <div class="card-img-top">
                    <% if organisation.cover_photo.attached? %>
                        <%= image_tag(organisation.cover_photo, alt: "Photo de couverture de l'association #{organisation.name}") %>
                    <% else %>
                        <%= image_tag("placeholder-image.png", alt: "Photo générique de couverture de l'association #{organisation.name}") %>
                    <% end %>
                </div>
                <h4><%= organisation.name %></h4>
                <p><%= organisation.description %></p>
            
            <% end %>
        <% end %>
    </div>
<% end %>

<br>

<h1 style="text-align: center; margin-top: 6px;">Les dernières associations</h1>
<p style="text-align: center;">Voici les nouvelles associations nous ayant rejoint :</p>
<p style="text-align: center;">
    <%= link_to "Voir l'ensemble des associations", organisations_path, style: "text-decoration: none;" %>
</p>
<hr style="width: 50%; margin-bottom: 30px;">

<div class="arrow-container">
    <div class="inline-card-container purple-container">
        <% if !@organisations.empty? %>
            <% @organisations.each do |organisation| %>
                <%= link_to organisation_path(organisation), class: "card" do %>
                
                    <div class="card-img-top">
                        <% if organisation.cover_photo.attached? %>
                            <%= image_tag(organisation.cover_photo, alt: "Photo de couverture de l'association #{organisation.name}") %>
                        <% else %>
                            <%= image_tag("placeholder-image.png", alt: "Photo générique de couverture de l'association #{organisation.name}") %>
                        <% end %>
                    </div>
                    <h4><%= organisation.name %></h4>
                    <p><%= organisation.description %></p>
                
                <% end %>
            <% end %>
        <% else %>
            <h5 style="text-align: center;">Aucune association pour le moment</h5>
        <% end %>
    </div>
</div>


<br>
<br>

<h1 style="text-align: center; margin-top: 6px;">Les derniers évènements</h1>
<p style="text-align: center;">Vous souhaitez donner de l'aide à une association? C'est l'occasion de participer à un évènement! 
<br>
<br>
Retrouvez les derniers évènements annoncés :</p>
<hr style="width: 50%; margin-bottom: 30px;">

<div class="inline-card-container blue-container">
    <% if !@events.empty? %>
        <% @events.each do |event| %>
            <%= link_to event_path(event), class: "card" do %>
            
                <div class="card-img-top">
                    <% if event.cover_photo.attached? %>
                        <%= image_tag(event.cover_photo, alt: "Photo de couverture de l'evenement #{event.name}") %>
                    <% else %>
                        <%= image_tag("placeholder-image.png", alt: "Photo générique de couverture de l'evenement #{event.name}") %>
                    <% end %>
                </div>
                <h4><%= event.name %></h4>
                <p class="mb-2"><%= event.description %></p>
                <p>Du : <%= event.start_date %></p>
                <p>Au : <%= event.end_date %></p>

                <div class="badge mt-3">
                    <% if event.participants.any? %>
                        <%= event.participants.count %>
                    <% else %>
                        0
                    <% end %>
                    participant<%= event.participants.count <= 1 ? "" : "s" %>
                </div>
            <% end %>
        <% end %>
    <% else %>
        <h5 style="margin-left: auto; margin-right: auto">Aucun evenement pour le moment</h5>
    <% end %>
</div>